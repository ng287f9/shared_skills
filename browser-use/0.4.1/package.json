{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@zcode/browser-use-plugin",
  "version": "0.4.1",
  "private": true,
  "description": "作为官方 ZCode 内置插件发布的 Browser Use skill、client runtime 与无状态 node_repl MCP server。",
  "license": "MIT",
  "type": "module",
  "main": "./dist/mcp/server.js",
  "scripts": {
    "build": "tsc && node scripts/build.mjs",
    "clean": "node ../../scripts/clean-dist.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run test",
    "lint": "oxlint src --no-ignore"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "@zcode/contracts": "workspace:*",
    "@zcode/core": "workspace:*",
    "@zcode/shared": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "2.0.0",
    "@types/node": "^24.0.0",
    "@zcode/adapters": "workspace:*",
    "@zcode/bootstrap": "workspace:*",
    "esbuild": "^0.25.0",
    "playwright-core": "1.59.1",
    "typescript": "^5.9.0",
    "vitest": "^4.1.5"
  }
}
